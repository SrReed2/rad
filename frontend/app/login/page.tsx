import LoginForm from "../../components/LoginForm";

export default function LoginPage() {
  return (
    <main className="min-h-screen bg-[#1A1A1F] flex items-center justify-center">
      <LoginForm />
    </main>
  );
}